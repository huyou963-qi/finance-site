import { EquityRegimeClient } from "@/components/equity/EquityRegimeClient";

export const metadata = { title: "宏观 Regime" };

export default function QuantRegimePage() {
  return <EquityRegimeClient />;
}
